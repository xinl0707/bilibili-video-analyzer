# UI 设计规范

## 一、设计风格

- **风格**：卡片圆润，类似 ChatGPT / Telegram
- **侧边栏宽度**：420px
- **圆角**：卡片 12px，按钮 8px，输入框 8px
- **阴影**：轻柔投影，层次分明

## 二、色彩系统

### 亮色主题

```
背景色
  主背景:     #f5f5f5
  卡片背景:   #ffffff
  输入框背景: #f0f0f0

文字色
  主文字:     #1a1a1a
  次要文字:   #666666
  占位文字:   #999999

强调色
  主强调:     #00a1d6（B站蓝）
  强调悬停:   #0088b9
  成功:       #52c41a
  警告:       #faad14
  错误:       #ff4d4f

边框
  边框色:     #e8e8e8
  分割线:     #f0f0f0
```

### 暗色主题

```
背景色
  主背景:     #111111
  卡片背景:   #1e1e1e
  输入框背景: #2a2a2a

文字色
  主文字:     #e5e5e5
  次要文字:   #a0a0a0
  占位文字:   #666666

强调色
  主强调:     #00a1d6（B站蓝，保持一致）
  强调悬停:   #26b5de
  成功:       #52c41a
  警告:       #faad14
  错误:       #ff4d4f

边框
  边框色:     #333333
  分割线:     #2a2a2a
```

## 三、字体系统

```css
字体族: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif

字号:
  标题:       16px / 600
  子标题:     14px / 600
  正文:       13px / 400
  辅助文字:   12px / 400
  按钮文字:   13px / 500

行高:
  正文:       1.6
  标题:       1.3
```

## 四、间距系统

```
基础单位: 4px

间距:
  xs:  4px
  sm:  8px
  md:  12px
  lg:  16px
  xl:  20px
  xxl: 24px

卡片内边距: 16px
区块间距:   12px
元素间距:   8px
```

## 五、组件设计

### 5.1 顶部标题栏

```
┌──────────────────────────────────────┐
│  🎵 B站视频分析          [🌙] [⚙️]  │
└──────────────────────────────────────┘

高度: 48px
背景: 卡片背景
底边框: 1px solid 边框色
左侧: 图标(20px) + 标题文字(16px/600)
右侧: 主题切换按钮(24px) + 设置按钮(24px)
按钮间距: 8px
```

### 5.2 视频信息卡片

```
┌──────────────────────────────────────┐
│  📺 这里是视频标题，最多显示两行...   │
│  UP主：xxx   时长：12:34             │
│  BV1xxxxxxxxx                        │
└──────────────────────────────────────┘

背景: 卡片背景
圆角: 12px
内边距: 16px
标题: 14px/600，最多2行，超出省略
信息行: 12px/400，次要文字色
图标: 16px，左对齐
```

### 5.3 进度区域

```
┌──────────────────────────────────────┐
│                                      │
│  ████████████░░░░░░░░  58%           │
│                                      │
│  你好，今天我们来聊聊最近比较火的     │
│  一个话题。首先我们要了解的是...      │
│                                      │
│  ⏱️ 已用时 00:23  预计剩余 ~35秒      │
│                                      │
└──────────────────────────────────────┘

进度条:
  高度: 6px
  圆角: 3px
  背景: #e8e8e8(亮) / #333333(暗)
  填充: 主强调色
  动画: 填充时平滑过渡 0.3s
  百分比: 右侧显示，12px/400

识别文字区:
  最大高度: 120px，超出滚动
  背景: 输入框背景
  圆角: 8px
  内边距: 12px
  字号: 13px/400
  行高: 1.6
  打字光标: 用 CSS 动画模拟闪烁

时间预估:
  字号: 12px/400
  颜色: 次要文字色
  间距: 与文字区间隔 8px
```

### 5.4 摘要卡片

```
┌──────────────────────────────────────┐
│  📋 视频摘要              [📋 复制]  │
│                                      │
│  这个视频主要讨论了以下几个方面：    │
│                                      │
│  1. 第一个要点的内容描述，可能会    │
│     比较长需要换行显示...            │
│                                      │
│  2. 第二个要点...                    │
│                                      │
│  3. 第三个要点...                    │
│                                      │
└──────────────────────────────────────┘

背景: 卡片背景
圆角: 12px
内边距: 16px
标题: 14px/600 + 复制按钮(右侧)
内容: 13px/400，行高 1.6
Markdown 渲染:
  标题: 加粗
  列表: 有序/无序列表样式
  代码: 等宽字体，背景色区分
  链接: 主强调色，可点击
复制按钮:
  24px 图标
  点击后变✓，1.5秒后恢复
```

### 5.5 对话区域

```
┌──────────────────────────────────────┐
│  💬 继续对话                          │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 👤 你能详细说说第二点吗？      │  │  ← 用户消息
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 🤖 好的，关于第二点，视频中    │  │  ← AI 回复
│  │ 提到了以下几个关键信息...      │  │
│  └────────────────────────────────┘  │
│                                      │
│  [关键要点] [争议观点] [补充细节]     │  ← 预设问题
│                                      │
│  ┌────────────────────────┐ [发送]   │
│  │ 输入你的问题...         │          │  ← 输入框
│  └────────────────────────┘          │
└──────────────────────────────────────┘

对话气泡:
  用户消息:
    背景: 主强调色(10%透明度)
    圆角: 12px (左上为 4px)
    对齐: 右对齐
    最大宽度: 85%
  AI 回复:
    背景: 卡片背景
    圆角: 12px (右上为 4px)
    对齐: 左对齐
    最大宽度: 85%
    边框: 1px solid 边框色
  气泡内边距: 12px
  气泡间距: 8px
  头像: 20px，用户👤/AI🤖

预设问题按钮:
  背景: 输入框背景
  边框: 1px solid 边框色
  圆角: 16px (胶囊形)
  字号: 12px/400
  内边距: 6px 12px
  间距: 8px，横向排列，超出换行
  悬停: 背景变为主强调色(10%透明度)

输入框:
  高度: 40px
  圆角: 8px
  内边距: 0 12px
  背景: 输入框背景
  边框: 1px solid 边框色
  聚焦: 边框变为主强调色
  发送按钮: 主强调色，圆角 8px，40px×40px
```

### 5.6 底部操作栏

```
┌──────────────────────────────────────┐
│  [📥 MD]  [📥 TXT]  [📥 HTML]  [🔄] │
└──────────────────────────────────────┘

高度: 48px
背景: 卡片背景
顶边框: 1px solid 边框色
按钮样式:
  背景: 输入框背景
  边框: 1px solid 边框色
  圆角: 8px
  字号: 12px/500
  内边距: 6px 12px
  间距: 8px
  悬停: 背景变为主强调色(10%透明度)
重试按钮:
  正常态: 同上
  错误态: 边框和文字变错误色
```

### 5.7 历史记录面板

```
┌──────────────────────────────────────┐
│  📚 分析历史              [清空]     │
├──────────────────────────────────────┤
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 📺 视频标题A                   │  │
│  │ UP主：xxx  2026-07-09 14:30    │  │
│  │ 点击展开查看摘要...            │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 📺 视频标题B                   │  │
│  │ UP主：yyy  2026-07-08 10:15    │  │
│  │ 点击展开查看摘要...            │  │
│  └────────────────────────────────┘  │
│                                      │
└──────────────────────────────────────┘

列表项:
  背景: 卡片背景
  圆角: 12px
  内边距: 12px 16px
  间距: 8px
  标题: 13px/600，单行省略
  信息: 12px/400，次要文字色
  摘要预览: 12px/400，单行省略
  悬停: 轻微阴影
  点击: 展开显示完整摘要
```

### 5.8 设置页面

```
┌──────────────────────────────────────┐
│  ⚙️ 设置                             │
├──────────────────────────────────────┤
│                                      │
│  AI 模型配置                         │
│  ┌────────────────────────────────┐  │
│  │ API Key                        │  │
│  │ [••••••••••••••••]             │  │
│  │                                │  │
│  │ Base URL                       │  │
│  │ [https://token-plan-cn...]     │  │
│  │                                │  │
│  │ 对话模型                       │  │
│  │ [mimo-v2.5-pro            ▾]  │  │
│  └────────────────────────────────┘  │
│                                      │
│  识别设置                            │
│  ┌────────────────────────────────┐  │
│  │ ASR 语言    [中文           ▾] │  │
│  │ MP3 码率    [96kbps         ▾] │  │
│  └────────────────────────────────┘  │
│                                      │
│  总结设置                            │
│  ┌────────────────────────────────┐  │
│  │ 总结风格    [默认           ▾] │  │
│  │                                │  │
│  │ 自定义提示词                   │  │
│  │ ┌────────────────────────────┐ │  │
│  │ │ 你是一个视频内容分析助手   │ │  │
│  │ │ 请用结构化的方式总结...    │ │  │
│  │ └────────────────────────────┘ │  │
│  └────────────────────────────────┘  │
│                                      │
│  快捷键                              │
│  ┌────────────────────────────────┐  │
│  │ 触发分析    [Alt + B      修改]│  │
│  └────────────────────────────────┘  │
│                                      │
└──────────────────────────────────────┘

分组:
  分组标题: 14px/600，主文字色
  分组间距: 20px
  卡片: 圆角 12px，内边距 16px

表单元素:
  标签: 12px/500，次文字色
  输入框: 高度 36px，圆角 8px
  下拉框: 高度 36px，圆角 8px
  开关: 自定义样式，主强调色
  文本域: 最少 4 行高
```

## 六、动画与过渡

```css
/* 通用过渡 */
transition: all 0.2s ease;

/* 进度条填充 */
transition: width 0.3s ease;

/* 气泡出现 */
@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
}
animation: fadeInUp 0.2s ease;

/* 打字光标闪烁 */
@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}
animation: blink 1s infinite;

/* 按钮点击反馈 */
transform: scale(0.97);
```

## 七、滚动条样式

```css
/* 暗色主题 */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #444; border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: #555; }

/* 亮色主题 */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #ccc; border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: #bbb; }
```

## 八、加载状态

### 骨架屏（初始加载）
```
┌──────────────────────────────────────┐
│  ████████████████  ████████          │  ← 灰色占位块
│  ████████████████████████████████    │
│  ████████████████                    │
└──────────────────────────────────────┘
用浅灰色块模拟内容位置，脉冲动画
```

### 分析中状态
```
┌──────────────────────────────────────┐
│  🔄 正在分析...                       │
│  ████████████░░░░░░░░  58%           │
│  识别文字逐字出现...|                 │
└──────────────────────────────────────┘
进度条带动画，文字有打字机效果
```

### 错误状态
```
┌──────────────────────────────────────┐
│  ❌ 识别失败                          │
│  错误信息：网络请求超时               │
│  [🔄 重试]                           │
└──────────────────────────────────────┘
红色图标 + 错误描述 + 重试按钮
```
